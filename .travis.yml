language: ruby
rvm:
  - 2.7.0
services:
  - postgresql
before_script:
  - psql -c 'create database nordicMapsApi_test;' -U postgres
script:
  - bundle exec rake db:migrate RAILS_ENV=test
  - bundle exec rspec
